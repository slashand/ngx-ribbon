dist: trusty

language: node_js
node_js:
  - '12'

branches:
  only:
    - master

addons:
  apt:
    sources:
      - google-chrome
    packages:
      - google-chrome-stable

cache: npm

cache:
  directories:
    - ./node_modules

before_script:
  - npm install


script:
  - npm run lint
  - npm run test
